apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: rag-api
spec:
  host: rag-api
  subsets:
    - name: stable
      labels:
        version: stable
    - name: canary
      labels:
        version: canary
