{
  "title": "LLM Demo - Exemplars",
  "panels": [
    {
      "type": "timeseries",
      "title": "p95 latency (with exemplars)",
      "targets": [
        { "expr": "histogram_quantile(0.95, sum(rate(app_request_latency_bucket[1m])) by (le))" }
      ],
      "options": { "exemplars": { "color": "inherit" } }
    }
  ],
  "timezone": "browser",
  "schemaVersion": 39
}
